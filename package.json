{
  "name": "websim-ai-agent",
  "version": "2.0.0",
  "description": "Multi-project AI agent for websim.com — give it a natural-language prompt and it edits your projects via any OpenAI-compatible endpoint.",
  "type": "commonjs",
  "scripts": {
    "agent": "node agent.js",
    "agent:chat": "node agent.js --interactive",
    "mcp": "node mcp-server.js",
    "projects": "node agent.js --list-projects",
    "check": "node -c agent.js && node -c mcp-server.js && node -c moderation.js && node static-checks.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "dotenv": "^16.6.1",
    "zod": "^3.23.8"
  }
}
